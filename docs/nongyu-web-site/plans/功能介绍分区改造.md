# 实施计划：农屿官网 · 功能介绍分区改造

| 项       | 内容                                             |
| -------- | ------------------------------------------------ |
| Spec     | `docs/nongyu-web-site/specs/功能介绍分区改造.md` |
| 应用     | `apps/nongyu-web-site`                           |
| 状态     | **已完成编码**                                   |
| 技术方案 | **已跳过**（用户确认）                           |

---

## 1. 实施计划

| 步骤 | 内容                                                            | 风险 |
| ---- | --------------------------------------------------------------- | ---- |
| 1    | 修订 `MASTER.md` 媒体比为 9/16                                  | 低   |
| 2    | 将 `assets/*.jpg` 拷入 `public/features/`（不含 mp4）           | 低   |
| 3    | `site-config`：六分区 + `images[]` + 占位文案；去掉主题块       | 低   |
| 4    | `main.ts`：渲染单图/轮播；pointer 拖滑 + 圆点；图片缺失占位     | 中   |
| 5    | `site.css`：竖屏设备窗、轮播轨道与指示点；桌面交替/移动堆叠保留 | 低   |
| 6    | 更新功能区 section 说明文案；`build` 冒烟                       | 低   |

**改动面**：`apps/nongyu-web-site/**`、`design-system/web-site/MASTER.md`、本计划与 Spec 状态。

---

## 2. 实施步骤（编码清单）

1. MASTER 媒体比例
2. 静态资源拷贝
3. site-config + main 渲染与轮播交互
4. CSS 竖屏窗 + carousel
5. Spec 标已实现；本地 build

---

## 3. 注意事项

- 媒体列内手机窗限宽（约 260–300px），避免 9:16 在半宽网格中过高
- `prefers-reduced-motion`：轮播瞬时切页
- 文案仅占位，产品方后续改 `site-config`
