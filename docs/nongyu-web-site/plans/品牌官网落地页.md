# 实施计划：农屿品牌官网落地页

| 项       | 内容                                           |
| -------- | ---------------------------------------------- |
| Spec     | `docs/nongyu-web-site/specs/品牌官网落地页.md` |
| 应用     | `apps/nongyu-web-site`                         |
| 状态     | **已完成编码**                                 |
| 技术方案 | **已跳过**（用户确认）                         |

---

## 1. 实施计划

| 步骤 | 内容                                                                               | 风险                          |
| ---- | ---------------------------------------------------------------------------------- | ----------------------------- |
| 1    | 补 `design-system/web-site/MASTER.md`（川农新绿色板 + 字体 + 签名元素）            | 低                            |
| 2    | Vite 工程：`vite.config.ts`、入口 `src/main.ts`、CSS Token、`site-config.ts`       | 低                            |
| 3    | 单页 DOM：Nav / Hero / Features / Download / Contact / Footer                      | 中：首屏构图与动效需对齐 Spec |
| 4    | 交互：锚点滚动、顶栏滚动态、复制反馈、Android URL 空禁用、`prefers-reduced-motion` | 低                            |
| 5    | GIF：`public/features/0x-*.gif` 缺失时占位；文档注明替换路径                       | 低                            |
| 6    | `pnpm --filter nongyu-web-site dev/build` 冒烟；Spec 标已实现                      | 低                            |

**栈**：Vite 8 + TypeScript + 原生 CSS（无 React/AntD；品牌站定制样式）。  
**改动面**：`apps/nongyu-web-site/**`、`design-system/web-site/**`、本计划与 Spec 状态。

---

## 2. 实施步骤（编码清单）

1. MASTER + site-config（文案、下载 URL、联系、功能六块 meta）
2. tokens.css + 全局排版与动效工具类
3. index.html 结构（语义化 section + id）
4. main.ts：nav scroll、copy、feature media 探测、reduce-motion
5. 本地 build 验证

---

## 3. 注意事项

- Android URL 默认空 → 「即将开放」
- GIF 未提交仓库时可仅 `.gitkeep`；运行时 `onerror`/存在性用占位
- 尊重用户「减少动态效果」
