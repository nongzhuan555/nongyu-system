# 实施计划：首页打招呼优化

| 项   | 内容                           |
| ---- | ------------------------------ |
| 日期 | 2026-08-16                     |
| 说明 | 跳过技术方案，按 Spec 直接编码 |

## 步骤

1. Node：`007_home_greetings.sql` + seed
2. Node：`home-greeting` 模块（repo / routes）挂到 `app.ts`
3. Node：集成测试 + `truncateAll` 纳入新表
4. RN：`fetchHomeGreeting` + `useGreetingBootstrap` + `GreetingSkeleton` + 改 `Greeting` / `greeting.ts`
5. Spec 状态改为已实现

## 风险

- 启用互斥依赖事务；测启用切换
- App `data: null` 需 `allowNullData`
