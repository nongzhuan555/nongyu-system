# nongyu-node-server 接口文档

> 状态：**已与实现同步**（以 `apps/nongyu-node-server` 当前代码为准，可直接对接）  
> 需求归类：基建需求  
> 依据：实现代码、`数据库设计文档.md`、原概览稿对齐结论  
> 服务：同一 Node 进程；`/api/app/*` 学生端，`/api/admin/*` 管理端  
> 同步日期：2026-08-14  
> 本地启动、测试账号、curl、双端联调及**开发期跨网临时隧道**见 [`联调指南.md`](./联调指南.md)（§2.6）

---

## 1. 背景与目标

### 1.1 背景

`nongyu-node-server` 为农屿单体业务后端，同时服务：

- `nongyu-rn-app`（学生端）
- `nongyu-web-admin`（管理端）

本服务**不校验教务密码**：学生端在 RN 内完成教务登录与个人信息抓取后，再调用 Node 完成「登录即注册 / 签发 Token」。管理端使用农屿管理员密码鉴权。

### 1.2 目标

- 统一双端鉴权、用户档案与设置同步
- 支撑广场（公告 / 反馈墙 / 大院）与阅读覆盖率
- 支撑 App 版本检查与管理端版本发布
- 支撑管理端用户管理与数据大屏（本库指标 + Track 指标 BFF）

### 1.3 非目标（本版不提供）

| 范围                      | 说明                                            |
| ------------------------- | ----------------------------------------------- |
| 埋点原始事件存储 / 日聚合 | 归 `nongyu-go-track-server`；管理端经 §6.6 读取 |
| 课表 CRUD                 | 课表领域表后置                                  |
| 教务 / 二课业务代理       | 客户端直连或后续单独接口集                      |
| Agent 会话                | 不落本库                                        |
| 管理员登录日志落库        | `loginType` 仅请求语义，不落库                  |

---

## 2. 通用约定

### 2.1 Base URL 与路径

- 业务前缀：`/api`
- 学生端：`/api/app/...`
- 管理端：`/api/admin/...`
- 内部：`/api/internal/...`（仅 Track 等内网服务，见 §7）
- 探活：`GET /health`（无 `/api` 前缀）
- Content-Type：`application/json; charset=utf-8`
- 时间字段：响应均为 **ISO-8601 UTC**（如 `2026-08-12T01:00:00.000Z`）
- JSON 字段：**camelCase**（库表 snake_case 由服务端映射）
- 响应头：每个请求回写 `x-request-id`（可主动传入同名请求头）

### 2.2 统一响应包

```ts
type ApiResponse<T> = {
  code: number; // 0 = 成功；非 0 = 业务/校验错误
  message: string;
  data: T | null;
};
```

成功：`{ "code": 0, "message": "ok", "data": ... }`  
失败：`{ "code": <非0>, "message": "...", "data": null }`

### 2.3 HTTP 状态码（已固定）

业务失败**不**用 HTTP 200 承载；一律按下列状态码 + `code`：

| HTTP | 场景                                                  |
| ---- | ----------------------------------------------------- |
| 200  | 成功（`code === 0`）                                  |
| 400  | 参数校验失败（`40001`）；登录限流亦返回本形态         |
| 401  | 未认证 / Token 无效 / 过期 / 作废（`40101`–`40104`）  |
| 403  | 无权限 / 账号禁用 / 管理员密码错误（`40301`–`40303`） |
| 404  | 资源不存在（`40401`–`40403`）                         |
| 502  | Track 指标代理失败（`50201`）                         |
| 503  | Track 不可达（`50301`）                               |
| 500  | 未捕获服务端错误（`50000`）                           |

### 2.4 业务错误码

| code  | 含义                                                      |
| ----- | --------------------------------------------------------- |
| 0     | 成功                                                      |
| 40001 | 参数校验失败（含学号格式、非法 gender、限流文案等）       |
| 40101 | 未认证（缺少/非法 `Authorization`）                       |
| 40102 | Token 无效（签名错误、载荷残缺、类型错误等）              |
| 40103 | Token 已过期（`exp`）                                     |
| 40104 | Token 已作废（顶号、登出、设备不符、用户不存在等）        |
| 40301 | 账号已禁用                                                |
| 40302 | 需要管理员权限 / 无权操作（含学生删非本人帖、超管未建档） |
| 40303 | 管理员密码错误                                            |
| 40401 | 用户不存在                                                |
| 40402 | 帖子不存在                                                |
| 40403 | 版本记录不存在                                            |
| 40404 | 管理员回复不存在（反馈墙未回复或已删）                    |
| 40405 | 留言不存在                                                |
| 40002 | 该帖子不允许留言（非大院帖）                              |
| 40304 | 无权删除该留言（非留言作者）                              |
| 40901 | 该反馈帖已存在管理员回复（一对一）                        |
| 50201 | 埋点指标查询失败（Track `ok: false` 或非预期响应）        |
| 50301 | 埋点服务暂不可用（超时、连接失败、5xx、Token 拒绝）       |
| 50000 | 服务器内部错误                                            |

### 2.5 鉴权

```http
Authorization: Bearer <jwt>
```

#### App JWT

- Claims：`uid`、`studentNo`、`tokenVersion`、`typ: "app"`、`deviceId`
- 校验：签名有效；`typ === "app"`；`users.status === 1`；`users.token_version === tokenVersion`；若 `users.current_device_id` 非空，则须等于 `deviceId`
- **同 `deviceId` 再次登录**：不 bump `token_version`（旧票仍可用）；**换设备**或库中设备为空后绑定新设备：`token_version += 1` 并更新设备字段
- 新用户首次登录：`token_version` 初始为 `1`
- 登出：`token_version += 1`，`is_online = 0`
- 默认 TTL：环境变量 `JWT_APP_TTL`（默认 `60d`）
- 超管学号（默认 `202308596`，见 `SUPER_ADMIN_STUDENT_NO`）App 登录时强制 `role=1`

#### Admin JWT

- Claims：`uid`、`studentNo`、`role: 1`、`typ: "admin"`；超管未建档时另含 `bootstrap: true` 且 `uid: 0`
- 校验：签名有效；`typ === "admin"`；bootstrap 票校验超管学号且不查库；否则 `users.role === 1` 且 `status === 1`
- **不设 `exp`（永不过期）**；**不踢号**（不依赖 `token_version`）
- 超管密码：`SUPER_ADMIN_DEFAULT_PASSWORD` 与 `admin_password_hash` **双轨命中**；无 `users` 行时仅默认密码，且**不写业务表**
- bootstrap 票仅可用于 `/api/admin/auth/me|logout|password`（改密无库时 403）；其它 `/api/admin/**` 返回 40302
- 自助改密：`PUT /api/admin/auth/password`

#### 内部 Token（Track 回写）

```http
X-Internal-Token: <INTERNAL_TOKEN>
```

- 环境变量 `INTERNAL_TOKEN`（≥16 字符），与 Track 的 `NODE_INTERNAL_TOKEN` 部署时对齐
- 常量时间比较；错误或缺失返回 HTTP 401，`code: 40101`

### 2.6 登录限流

`POST /api/app/auth/login` 与 `POST /api/admin/auth/login`：每 IP **15 分钟最多 60 次**。超限返回 HTTP 400，`code: 40001`，`message: 登录尝试过于频繁，请稍后再试`。

### 2.7 分页

| 参数       | 类型   | 默认 | 说明                       |
| ---------- | ------ | ---- | -------------------------- |
| `page`     | number | 1    | 从 1 开始                  |
| `pageSize` | number | 20   | 最大 **100**（超出按 100） |

```ts
type PageResult<T> = {
  list: T[];
  total: number;
  page: number;
  pageSize: number;
};
```

### 2.8 枚举与映射

| 字段                  | 值                                                              |
| --------------------- | --------------------------------------------------------------- |
| `gender`              | `0` 未知 / `1` 男 / `2` 女                                      |
| `role`                | `0` 普通用户 / `1` 管理员                                       |
| `status`              | `1` 正常 / `0` 禁用                                             |
| `postType`            | `announcement` / `feedback` / `courtyard`                       |
| `loginType`           | `browser` / `in_app`                                            |
| `platform`            | `ios` / `android` / `all`（版本检查 Query 仅 `ios`\|`android`） |
| `releaseChannel`      | `silent_ota` / `native`                                         |
| `theme`               | `sicau_green` / `sakura_pink` / `dark` / `system`               |
| `courseCardColorMode` | `distinct` / `unified`                                          |

**App 登录 `gender` 入参**：允许 `0|1|2` 或 `"0"|"1"|"2"|"男"|"女"|"未知"`。  
未传 / `null` / `""` → 落库 `0`。  
其它非法值 → **400 + 40001**（`gender 无效`），不会静默落 0。

**帖子 `subtype`**：实现侧校验为非空字符串、`≤32`；**不强制**文档历史上的枚举名单（`system`/`feedback`/`bug` 等由客户端约定即可）。

---

## 3. 核心业务流程

### 3.1 App 登录即注册

```text
RN 教务校验通过并抓取档案
  → POST /api/app/auth/login（档案 + 设备信息；不传教务密码）
  → 无用户则 INSERT + 默认 user_settings；有则刷新档案/设备（同设备不 bump tokenVersion；换设备则 bump）
  → 签发 App JWT
```

- 未知字段（如 `role` / `status` / `adminPassword`）会被忽略（Zod strip）
- `qq`：库中已有非空值则**不覆盖**；库空时可用请求体写入
- 新用户同时创建默认 `user_settings` 行
- 超管学号登录时强制 `role=1`

### 3.2 管理端登录

```text
POST /api/admin/auth/login（studentNo + adminPassword [+ loginType]）
  → 超管学号：默认密码或哈希双轨；无 users 行则 bootstrap 票（不写表）
  → 非超管：用户不存在 40401；role !== 1 → 40302；status !== 1 → 40301；密码不匹配 → 40303
  → 签发 Admin JWT（无 exp）；loginType 仅回显
```

### 3.3 广场阅读量

打开 `GET /api/app/posts/:id` 成功时（事务）：

1. `posts.view_count += 1`
2. 若 `(post_id, user_id)` 首次写入 `post_reads`：再 `unique_reader_count += 1`
3. 覆盖率（管理端）= `unique_reader_count / COUNT(users)`

App 列表/详情**不返回**阅读量；「我的帖子」返回 `viewCount`。

---

## 4. 探活

### `GET /health`

**鉴权**：无

**成功 `data`**：

```ts
{
  status: "ok";
  db: "up" | "down";
}
```

---

## 5. 学生端接口（`/api/app`）

### 5.1 鉴权

#### `POST /api/app/auth/login`

**鉴权**：无（有登录限流）

**请求体**：

| 字段                              | 类型             | 必填 | 约束              |
| --------------------------------- | ---------------- | ---- | ----------------- |
| `studentNo`                       | string           | 是   | 恰好 9 位数字     |
| `name`                            | string           | 是   | 1–64              |
| `major` / `college` / `className` | string           | 否   | ≤128              |
| `grade`                           | string           | 否   | ≤32               |
| `gender`                          | number \| string | 否   | 见 §2.8           |
| `hometown` / `campus`             | string           | 否   | ≤64               |
| `qq`                              | string           | 否   | ≤20；仅库空时写入 |
| `deviceId`                        | string           | 是   | 1–128             |
| `deviceBrand`                     | string           | 否   | ≤64               |
| `deviceModel`                     | string           | 否   | ≤128              |
| `deviceOs`                        | string           | 否   | ≤64               |

**成功 `data`**：

```ts
{
  token: string;
  isNewUser: boolean;
  user: AppUserProfile;
}
```

**失败**：学号非法 `40001`；账号禁用 `40301`；gender 非法 `40001`。

---

#### `POST /api/app/auth/logout`

**鉴权**：App JWT  
**行为**：`token_version += 1`，`is_online = 0`  
**成功 `data`**：`null`

---

#### `GET /api/app/auth/me`

**鉴权**：App JWT  
**成功 `data`**：`AppUserProfile`

---

### 5.2 用户资料

```ts
type AppUserProfile = {
  id: number;
  studentNo: string;
  name: string;
  major: string | null;
  college: string | null;
  className: string | null;
  grade: string | null;
  gender: 0 | 1 | 2;
  hometown: string | null;
  campus: string | null;
  qq: string | null;
  role: 0 | 1;
  createdAt: string; // ISO UTC
};
```

#### `GET /api/app/users/me`

**鉴权**：App JWT → `AppUserProfile`

#### `PATCH /api/app/users/me`

**鉴权**：App JWT  
**请求体**：`{ "qq": string }`（≤20；空串清空为 `null`）  
**成功 `data`**：`AppUserProfile`

---

### 5.3 用户设置

```ts
type UserSettings = {
  theme: "sicau_green" | "sakura_pink" | "dark" | "system";
  homeIsTimetable: boolean;
  openWebInApp: boolean;
  agentEnabled: boolean;
  highlightTodayColumn: boolean;
  courseCardColorMode: "distinct" | "unified";
  courseCardUnifiedColor: string | null;
  semesterStartDate: string | null; // YYYY-MM-DD
  timetableBgUri: string | null;
  updatedAt: string;
};
```

大模型 API Key **不落库、不经本接口**。

#### `GET /api/app/settings`

**鉴权**：App JWT  
无行则惰性插入默认行后返回。

#### `PUT /api/app/settings`

**鉴权**：App JWT  
**请求体**：`Partial<UserSettings>`（不含 `updatedAt`）；未传字段保持原值  
**成功 `data`**：完整 `UserSettings`

---

### 5.4 广场

```ts
type PostListItem = {
  id: number;
  postType: "announcement" | "feedback" | "courtyard";
  subtype: string;
  title: string;
  contentPreview: string; // 去 HTML 标签后截断约 120 字
  publishedAt: string;
  authorDisplayName?: string | null; // App 端恒为 null（反馈墙/大院对用户匿名）；署名仅 Admin 接口返回
};

type PostDetail = {
  id: number;
  postType: "announcement" | "feedback" | "courtyard";
  subtype: string;
  title: string;
  content: string;
  publishedAt: string;
  authorDisplayName?: string | null; // 同上，恒为 null
  isMine: boolean; // 仅供本人删帖；不向其他用户暴露作者身份
  // 反馈墙：管理员单条回复（用户侧统一「管理员回复」，无作者信息）
  adminReply?: { content: string; publishedAt: string } | null;
  // 大院：顶层留言列表（完全匿名，仅 isMine 自识）
  comments?: Array<{
    id: number;
    content: string;
    publishedAt: string;
    isMine: boolean;
  }>;
};

// 「我的帖子」列表项额外字段（普通广场列表不返回）
type MyPostListItemExtra = {
  replyCount: number; // 未软删回复总数（含 admin_reply 与 comment）
  hasReply: boolean; // replyCount > 0
};

// 轮询「我的新回复」单项
type NewPostReply = {
  replyId: number;
  postId: number;
  postType: "announcement" | "feedback" | "courtyard";
  postTitle: string;
  kind: "admin_reply" | "comment";
  content: string;
  createdAt: string;
};
```

#### `GET /api/app/posts`

**鉴权**：App JWT  
**Query**：`postType`（必填）、`page`/`pageSize`、`subtype?`（精确）、`keyword?`（可选，见下）  
排序：`publishedAt DESC`  
**成功 `data`**：`PageResult<PostListItem>`

**`keyword`（广场搜索）**：

- 未传 / 空白（trim 后空）：行为与无关键词列表一致
- 有值：trim 后长度 **1–64**；对 `title`、`content` 做转义后的 `LIKE %kw%`（两字段 OR），再与 `postType` / `subtype` **AND**
- 非法长度 → HTTP 400，`code: 40001`
- 软删帖永不返回；不跨分类（`postType` 仍必填）
- 规格：`docs/nongyu-node-server/specs/广场帖子搜索.md`

#### `GET /api/app/posts/announcements/latest`

**鉴权**：App JWT  
**成功 `data`**：`{ id, title, subtype, publishedAt } | null`

#### `GET /api/app/posts/:id`

**鉴权**：App JWT  
成功打开即计阅读（§3.3）；**不返回** `viewCount` / `uniqueReaderCount`  
**成功 `data`**：`PostDetail`，并按 `postType` 附加：

- `feedback`：`adminReply`（无则 `null`）
- `courtyard`：`comments`（按 `publishedAt ASC`，软删剔除）
- `announcement`：均不附加

#### `POST /api/app/posts/:id/comments`

**鉴权**：App JWT  
**约束**：仅 `postType=courtyard` 的帖子可留言；非大院帖 → HTTP 400，`code: 40002`，`message: 该帖子不允许留言`  
**请求体**：

| 字段      | 必填 | 说明              |
| --------- | ---- | ----------------- |
| `content` | 是   | trim 后 1–1000 字 |

**成功 `data`**：`{ id: number, content: string, publishedAt: string, isMine: true }`

#### `DELETE /api/app/posts/:id/comments/:commentId`

**鉴权**：App JWT  
仅留言作者本人可删；非作者 → HTTP 403，`code: 40304`，`message: 无权删除该留言`  
**成功 `data`**：`null`

#### `POST /api/app/posts`

**鉴权**：App JWT  
**请求体**：

| 字段       | 必填 | 说明                         |
| ---------- | ---- | ---------------------------- |
| `postType` | 是   | 仅 `feedback` \| `courtyard` |
| `subtype`  | 是   | 1–32 字符                    |
| `title`    | 是   | 1–200                        |
| `content`  | 是   | 非空                         |

**成功 `data`**：`{ id: number }`

#### `DELETE /api/app/posts/:id`

**鉴权**：App JWT  
作者软删自己的 `feedback`/`courtyard`。  
非作者或公告 → HTTP 403，`code: 40302`，`message: 无权删除该帖子`  
**成功 `data`**：`null`

#### `GET /api/app/users/me/posts`

**鉴权**：App JWT  
**Query**：`page`/`pageSize`；可选 `postType=feedback|courtyard`（不传则两类都查，不含公告）  
**成功 `data`**：`PageResult<PostListItem & { viewCount: number } & MyPostListItemExtra>`

#### `GET /api/app/users/me/post-replies/new`

**鉴权**：App JWT  
轮询当前登录用户**自己帖子**下尚未通知的新回复（含管理员回复与用户留言）。  
**成功 `data`**：`NewPostReply[]`（按 `createdAt ASC`，最多 50 条）  
**副作用**：返回项的 `notified_author` 置 1；后续轮询不再返回。  
**说明**：不返回他人帖子下的回复；帖子被软删后其回复不再返回。

---

### 5.5 App 版本

#### `GET /api/app/versions/check`

**鉴权**：**无**（公开）

**Query**：

| 参数          | 必填 | 说明               |
| ------------- | ---- | ------------------ |
| `platform`    | 是   | `ios` \| `android` |
| `versionCode` | 是   | int                |

逻辑：取 `is_published=1` 且 `platform IN (<query>, 'all')` 中 `version_code` 最大的一条，与本地比较。

**成功 `data`**：

```ts
{
  hasUpdate: boolean;
  forceUpdate: boolean;
  latest: null | {
    versionName: string;
    versionCode: number;
    releaseChannel: "silent_ota" | "native";
    downloadUrl: string | null;
    changelog: string | null;
  };
}
```

无更新时：`hasUpdate=false`，`forceUpdate=false`，`latest=null`。

---

## 6. 管理端接口（`/api/admin`）

除登录外均需 **Admin JWT**。

### 6.1 鉴权

#### `POST /api/admin/auth/login`

**鉴权**：无（有登录限流）

| 字段            | 必填 | 说明                                  |
| --------------- | ---- | ------------------------------------- |
| `studentNo`     | 是   | 9 位数字                              |
| `adminPassword` | 是   | 农屿管理员密码                        |
| `loginType`     | 否   | `browser` \| `in_app`；缺省 `browser` |

**成功 `data`**：

```ts
{
  token: string;
  loginType: "browser" | "in_app";
  user: {
    id: number;
    studentNo: string;
    name: string;
    role: 1;
    bootstrap?: true; // 超管未建档
  }
}
```

#### `POST /api/admin/auth/logout`

**鉴权**：Admin JWT（含 bootstrap）  
服务端无黑名单；客户端丢弃 Token 即可。  
**成功 `data`**：`null`

#### `GET /api/admin/auth/me`

**鉴权**：Admin JWT（含 bootstrap）  
**成功 `data`**：`{ id, studentNo, name, role: 1, bootstrap?: true }`

#### `PUT /api/admin/auth/password`

**鉴权**：Admin JWT（**非** bootstrap）

| 字段            | 必填 | 说明       |
| --------------- | ---- | ---------- |
| `adminPassword` | 是   | 新密码，≥8 |

写入当前用户 `admin_password_hash`；不吊销已发 Admin JWT。超管改密后出厂默认密码仍可登录。  
**成功 `data`**：`null`

#### `POST /api/admin/auth/app-handoff`

**鉴权**：App JWT（有登录限流）  
**资格**：对应用户已建档、`role === 1`、`status === 1`（不支持超管 bootstrap 无库态）

**成功 `data`**：

```ts
{
  ticket: string; // 不透明，URL-safe；TTL 60s；单次
  expiresIn: number; // 秒，固定 60
}
```

| 情况     | HTTP | code    |
| -------- | ---- | ------- |
| 非管理员 | 403  | `40302` |
| 账号禁用 | 403  | `40301` |

#### `POST /api/admin/auth/handoff-redeem`

**鉴权**：无（有登录限流）  
**请求体**：`{ "ticket": string }`

**成功 `data`**：与 `POST /api/admin/auth/login` 成功结构对齐；`loginType` 固定 `"in_app"`；无 `bootstrap`。

| 情况                 | HTTP | code    |
| -------------------- | ---- | ------- |
| 缺 / 空 ticket       | 400  | `40001` |
| 无效 / 过期 / 已使用 | 401  | `40102` |

---

### 6.2 用户管理

```ts
type AdminUserListItem = {
  id: number;
  studentNo: string;
  name: string;
  college: string | null;
  grade: string | null;
  campus: string | null;
  role: 0 | 1;
  status: 0 | 1;
  isOnline: boolean;
  lastLoginAt: string | null;
  createdAt: string;
};
```

#### `GET /api/admin/users`

**Query**：`page`/`pageSize`；`keyword?`（学号/姓名模糊）；`role?`；`status?`  
**成功 `data`**：`PageResult<AdminUserListItem>`

#### `GET /api/admin/users/:id`

**成功 `data`**：`AppUserProfile` 扩展字段 + 设置摘要：

```ts
AppUserProfile & {
  status: 0 | 1;
  isOnline: boolean;
  lastActiveAt: string | null;
  lastLoginAt: string | null;
  deviceBrand: string | null;
  deviceModel: string | null;
  deviceOs: string | null;
  currentDeviceId: string | null;
  settings: UserSettings;
}
```

#### `PATCH /api/admin/users/:id`

**请求体**：`{ role?: 0|1; status?: 0|1 }`

- `role` 从 `1`→`0`：清空 `admin_password_hash`
- **不可**在本接口同请求设置密码；升为管理员后须再调 `PUT .../admin-password`

**成功 `data`**：`{ id, studentNo, name, role, status }`

#### `PUT /api/admin/users/:id/admin-password`

目标用户必须已是 `role === 1`。  
**请求体**：`{ "adminPassword": string }`（**≥ 8** 位）  
**成功 `data`**：`null`

---

### 6.3 广场管理

```ts
type AdminPostItem = {
  id: number;
  postType: "announcement" | "feedback" | "courtyard";
  subtype: string;
  title: string;
  content: string;
  authorUserId: number;
  authorStudentNo: string;
  authorName: string;
  viewCount: number;
  uniqueReaderCount: number;
  coverageRate: number; // uniqueReaderCount / totalUsers
  publishedAt: string;
  deletedAt: string | null;
  replyCount: number; // 未软删回复总数（含 admin_reply 与 comment）
  // 详情附加（仅 GET /:id）：
  adminReply?: {
    id: number;
    content: string;
    publishedAt: string;
    authorUserId: number;
    authorStudentNo: string;
    authorName: string;
  } | null;
  comments?: Array<{
    id: number;
    content: string;
    publishedAt: string;
    authorUserId: number;
    authorStudentNo: string;
    authorName: string;
  }>;
};
```

#### `GET /api/admin/posts`

**Query**：`postType?`（可选）、`subtype?`（精确）、`keyword?`（同 App 列表语义）、`page`/`pageSize`、`includeDeleted?`（默认 false）  
**成功 `data`**：`PageResult<AdminPostItem>`

`keyword` 与 `includeDeleted` 可同时生效；未传 `postType` 时可在全类型范围内按关键词检索。

#### `GET /api/admin/posts/:id`

**成功 `data`**：`AdminPostItem`，并按 `postType` 附加：

- `feedback`：`adminReply`（无则 `null`，含管理员实名信息）
- `courtyard`：`comments`（按 `publishedAt ASC`，含留言者实名信息）
- `announcement`：均不附加

#### `POST /api/admin/posts/:id/reply`

对反馈墙帖子创建管理员回复（一对一）。  
**约束**：仅 `postType=feedback` 可回复；已存在未软删回复 → HTTP 409，`code: 40901`，`message: 该反馈帖已存在管理员回复`  
**请求体**：

| 字段      | 必填 | 说明              |
| --------- | ---- | ----------------- |
| `content` | 是   | trim 后 1–2000 字 |

**成功 `data`**：`{ id: number, content: string, publishedAt: string }`

#### `PATCH /api/admin/posts/:id/reply`

更新当前管理员回复内容（任意管理员可改，不留痕）。  
**约束**：帖子须为 `feedback` 且存在未软删回复；否则 HTTP 404，`code: 40404`  
**请求体**：`{ content: string }`（trim 后 1–2000）  
**成功 `data`**：`{ id: number, content: string, updatedAt: string }`  
**说明**：编辑**不重置** `notified_author`（已通知过的作者不会因编辑再次收到提醒）。

#### `DELETE /api/admin/posts/:id/reply`

软删当前管理员回复。  
**约束**：帖子须为 `feedback` 且存在未软删回复；否则 HTTP 404，`code: 40404`  
**成功 `data`**：`null`

#### `DELETE /api/admin/posts/:id/comments/:commentId`

管理员删除任意用户留言（软删）。  
**约束**：留言不存在或已软删 → HTTP 404，`code: 40405`  
**成功 `data`**：`null`

#### `POST /api/admin/posts`

发布公告（服务端固定 `postType=announcement`）。

| 字段          | 必填 | 说明                                              |
| ------------- | ---- | ------------------------------------------------- |
| `subtype`     | 是   | 1–32                                              |
| `title`       | 是   | 1–200                                             |
| `content`     | 是   | 非空                                              |
| `publishedAt` | 否   | ISO datetime（`z.string().datetime()`）；默认现在 |

**成功 `data`**：`{ id: number }`

#### `PATCH /api/admin/posts/:id`

仅允许编辑**未软删的公告**；其它类型 → `40001`「本版仅允许编辑公告」。  
可改：`subtype` / `title` / `content` / `publishedAt`  
**成功 `data`**：`{ id: number }`

#### `DELETE /api/admin/posts/:id`

软删（公告/反馈/大院均可）。  
**成功 `data`**：`null`

---

### 6.4 App 版本管理

```ts
type AdminAppVersion = {
  id: number;
  platform: "ios" | "android" | "all";
  versionName: string;
  versionCode: number;
  releaseChannel: "silent_ota" | "native";
  forceUpdate: boolean;
  downloadUrl: string | null;
  changelog: string | null;
  isPublished: boolean;
  publishedAt: string | null;
  createdAt: string;
  updatedAt: string;
};
```

#### `GET /api/admin/app-versions`

**Query**：`platform?`、`isPublished?`（`true`/`false`）、`page`/`pageSize`  
**成功 `data`**：`PageResult<AdminAppVersion>`

#### `POST /api/admin/app-versions`

**请求体**：`platform`、`versionName`、`versionCode`、`releaseChannel`、`forceUpdate?`（默认 false）、`downloadUrl?`、`changelog?`、`isPublished?`（默认 false）  
发布时写入 `publishedAt`。  
**成功 `data`**：`{ id: number }`

#### `PATCH /api/admin/app-versions/:id`

可更新上述字段；`isPublished=true` 时 `publishedAt = COALESCE(publishedAt, now)`。  
**成功 `data`**：完整 `AdminAppVersion`

#### `DELETE /api/admin/app-versions/:id`

**仅下架**：`isPublished=false`，**不物理删除**。  
**成功 `data`**：`null`

---

### 6.5 数据大屏（本库指标）

> 业务日时区：环境变量 `BUSINESS_TZ`，默认 **`Asia/Shanghai`**。  
> DAU / 页面 / 性能 / 崩溃：见 §6.6 Track 代理。

#### `GET /api/admin/dashboard/overview`

```ts
{
  totalUsers: number;
  totalAdmins: number; // role=1
  onlineUsers: number; // is_online=1 且 last_active_at 在近 10 分钟内（与 Track 心跳超时对齐；过期行会顺带清成离线）
  todayNewUsers: number; // created_at 落在业务日 [start, end)
}
```

#### `GET /api/admin/dashboard/user-growth`

**Query**：`range=7d|30d|90d|180d|365d`（默认 `7d`）  
**成功 `data`**：`{ points: { date: string; /* YYYY-MM-DD */ newUsers: number }[] }`

#### `GET /api/admin/dashboard/user-distribution`

```ts
{
  gender: {
    key: "male" | "female" | "unknown";
    count: number;
  }
  [];
  campus: {
    key: string;
    count: number;
  }
  []; // 空值归 unknown
  college: {
    key: string;
    count: number;
  }
  [];
  grade: {
    key: string;
    count: number;
  }
  [];
  deviceBrand: {
    key: string;
    count: number;
  }
  [];
}
```

#### `GET /api/admin/dashboard/settings-distribution`

```ts
{
  theme: {
    key: string;
    count: number;
  }
  [];
  homeIsTimetable: {
    key: "true" | "false";
    count: number;
  }
  [];
  openWebInApp: {
    key: "true" | "false";
    count: number;
  }
  [];
  agentEnabled: {
    key: "true" | "false";
    count: number;
  }
  [];
}
```

---

### 6.6 数据大屏（Track 指标代理）

> 管理端 **禁止** 直连 Track。Node 使用 `TRACK_BASE_URL`（默认 `http://127.0.0.1:8082`）与 `INTERNAL_TOKEN`（`X-Internal-Token`，对齐 Track `INTERNAL_TOKEN`）转发。  
> 对外仍为 `{ code, message, data }` + camelCase。缺省日期 = `BUSINESS_TZ` 当天。  
> Track 不可达 / 5xx / 403：HTTP 503，`code=50301`。其它上游失败：HTTP 502，`code=50201`。  
> 响应 **不含** Track 的 `online`（当前在线只读 §6.5 overview）。

#### `GET /api/admin/track/overview`

**Query**：`date=YYYY-MM-DD`（可选）

```ts
{
  date: string;
  dau: number;
  crashCount: number;
  appOpenCount: number;
  screenViewCount: number;
  buttonClickCount?: number;
}
```

#### `GET /api/admin/track/dims`

**Query**：`metric=screen_views|screen_dwell_avg|button_clicks|perf_p50|perf_p95`（必填）；`date` 可选；`limit` 默认 50、最大 100。`screen_views` 为进入次数；`screen_dwell_avg` 为页均停留（ms）。

```ts
{
  date: string;
  metric: string;
  items: {
    dimKey: string;
    dimValue: string;
    metricValue: number;
  }
  [];
}
```

#### `GET /api/admin/track/crashes`

**Query**：`from` / `to` 可选（缺省当天）；`page` / `pageSize`（默认 20，最大 100）。`from > to` → 400。

**成功 `data`**：`PageResult<CrashItem>`

```ts
type CrashItem = {
  eventId: string;
  userId: number | null;
  studentNo: string | null;
  eventName: string;
  appVersion: string | null;
  platform: string | null;
  deviceBrand: string | null;
  clientTsMs: number | null;
  receivedAtMs: number;
  statDate: string;
  props: Record<string, unknown> | null;
};
```

#### `GET /api/admin/track/llm-proxy-fails`

**Query**：`from` / `to` 可选（缺省当天）；`page` / `pageSize`；`errorCode` 可选（`50210|50310|50311|42910|42911`）。

**成功 `data`**：同 crashes 的 `PageResult`（`eventName` 为错误码；`props` 含 `error_message` / `attempts` 等）。

#### `GET /api/admin/track/trend`

**Query**：`metric=dau|crash_count|app_open_count|screen_view_count|online_peak`；`from` / `to` 必填。

**成功 `data`**：`{ points: { date: string; value: number }[] }`（缺日不补零）

#### `POST /api/admin/track/query`

- 鉴权：已建档管理员
- Body：`{ sql: string }`（1–8000 字符）
- 转发 Track `POST /v1/admin/sql/query`（Internal Token）；浏览器禁止直连 Track
- 成功 `data`：`{ sql, columns, rows, truncated, rowCount }`
- Track `INVALID_SQL` → HTTP 400，`code=40001`
- Track 不可达 / 5xx：同其它 track 代理 `50301` / `50201`

---

## 6.x 平台 LLM Key 池与代理

契约真相源：`docs/nongyu-node-server/specs/平台LLM密钥池与代理.md`。

### App

#### `POST /api/app/llm/v1/chat/completions`

- 鉴权：App JWT（`Authorization: Bearer <token>`）
- OpenAI Chat Completions 兼容；支持 `stream`；上游 `model`/`baseUrl` 取自当前调度 Key（空则回落 `LLM_POOL_DEFAULT_MODEL` / `LLM_POOL_DEFAULT_BASE_URL`）；忽略客户端 `model`
- 失败换 Key 为**串行**（非并行扇出）；单次请求对客户端至多一路成功响应；流式一旦开始写出则不再换 Key
- 业务失败码：`42910` / `42911` / `50310` / `50311` / `50210`（详见 Spec）
- 业务错误码：`42910` 日限、`42911` 用户忙、`50310` 池不可用、`50311` 繁忙、`50210` 上游失败
- 上述业务失败时 Node **异步**向 Track `POST /v1/internal/events` 上报 `llm_proxy_fail`（失败不影响本接口响应）

#### `POST /api/admin/llm/v1/chat/completions`

- 鉴权：已建档管理员 JWT（bootstrap 票 40302）
- 行为与 App `POST /api/app/llm/v1/chat/completions` 相同（同 Key 池、同错误码、同日限按 `uid`）

### Admin

#### `GET/POST /api/admin/llm/keys`、`PATCH/DELETE /api/admin/llm/keys/:id`

- 鉴权：已建档管理员
- 列表/详情永不返回明文 Key，仅 `apiKeySuffix`
- 分页 `data`：`{ list, total, page, pageSize }`

---

## 7. 内部接口（Track 回写）

仅内网 / 本机调用，**禁止**对公网暴露。鉴权见 §2.5 内部 Token。字段为 **snake_case**（对齐 Track）。

### `POST /api/internal/users/presence`

| 字段                | 必填 | 说明                                  |
| ------------------- | ---- | ------------------------------------- |
| `user_id`           | 是   | 业务用户 id                           |
| `is_online`         | 是   | `0` / `1`                             |
| `last_active_at_ms` | 是   | UTC 毫秒；写入 `users.last_active_at` |

**成功 `data`**：`{ userId, isOnline }`  
**404**：用户不存在（`40401`）

---

## 8. 接口清单速查

### 公共

| 方法 | 路径      | 鉴权 | 说明           |
| ---- | --------- | ---- | -------------- |
| GET  | `/health` | 无   | 探活 + DB ping |

### App

| 方法   | 路径                                  | 鉴权 | 说明               |
| ------ | ------------------------------------- | ---- | ------------------ |
| POST   | `/api/app/auth/login`                 | 无   | 登录即注册（限流） |
| POST   | `/api/app/auth/logout`                | App  | 登出               |
| GET    | `/api/app/auth/me`                    | App  | 会话校验           |
| GET    | `/api/app/users/me`                   | App  | 资料               |
| PATCH  | `/api/app/users/me`                   | App  | 改 QQ              |
| GET    | `/api/app/settings`                   | App  | 读设置             |
| PUT    | `/api/app/settings`                   | App  | 写设置             |
| GET    | `/api/app/posts`                      | App  | 广场列表           |
| GET    | `/api/app/posts/announcements/latest` | App  | 最新公告           |
| GET    | `/api/app/posts/:id`                  | App  | 详情+计阅读        |
| POST   | `/api/app/posts`                      | App  | 发反馈/大院        |
| DELETE | `/api/app/posts/:id`                  | App  | 软删自己的帖       |
| GET    | `/api/app/users/me/posts`             | App  | 我的帖子           |
| GET    | `/api/app/versions/check`             | 无   | 版本检查           |
| POST   | `/api/app/llm/v1/chat/completions`    | App  | 平台 LLM 代理      |

### Admin

| 方法   | 路径                                         | 鉴权  | 说明                      |
| ------ | -------------------------------------------- | ----- | ------------------------- |
| POST   | `/api/admin/auth/login`                      | 无    | 管理员登录（限流）        |
| POST   | `/api/admin/auth/app-handoff`                | App   | App→管理台 handoff ticket |
| POST   | `/api/admin/auth/handoff-redeem`             | 无    | ticket 兑换 Admin 会话    |
| POST   | `/api/admin/auth/logout`                     | Admin | 登出                      |
| GET    | `/api/admin/auth/me`                         | Admin | 当前管理员                |
| PUT    | `/api/admin/auth/password`                   | Admin | 自助改密（非 bootstrap）  |
| GET    | `/api/admin/users`                           | Admin | 用户列表                  |
| GET    | `/api/admin/users/:id`                       | Admin | 用户详情                  |
| PATCH  | `/api/admin/users/:id`                       | Admin | 改角色/状态               |
| PUT    | `/api/admin/users/:id/admin-password`        | Admin | 设管理员密码              |
| GET    | `/api/admin/posts`                           | Admin | 帖子列表                  |
| GET    | `/api/admin/posts/:id`                       | Admin | 帖子详情                  |
| POST   | `/api/admin/posts`                           | Admin | 发公告                    |
| PATCH  | `/api/admin/posts/:id`                       | Admin | 编辑公告                  |
| DELETE | `/api/admin/posts/:id`                       | Admin | 软删                      |
| GET    | `/api/admin/app-versions`                    | Admin | 版本列表                  |
| POST   | `/api/admin/app-versions`                    | Admin | 创建版本                  |
| PATCH  | `/api/admin/app-versions/:id`                | Admin | 更新/发布                 |
| DELETE | `/api/admin/app-versions/:id`                | Admin | 下架                      |
| GET    | `/api/admin/dashboard/overview`              | Admin | 大屏概览                  |
| GET    | `/api/admin/dashboard/user-growth`           | Admin | 增长趋势                  |
| GET    | `/api/admin/dashboard/user-distribution`     | Admin | 用户分布                  |
| GET    | `/api/admin/dashboard/settings-distribution` | Admin | 设置分布                  |
| GET    | `/api/admin/track/overview`                  | Admin | 埋点概览（DAU 等）        |
| GET    | `/api/admin/track/dims`                      | Admin | 埋点维度分布              |
| GET    | `/api/admin/track/crashes`                   | Admin | 崩溃列表                  |
| GET    | `/api/admin/track/llm-proxy-fails`           | Admin | 平台 LLM 代理失败列表     |
| GET    | `/api/admin/track/trend`                     | Admin | 埋点趋势                  |
| POST   | `/api/admin/track/query`                     | Admin | Track 只读 SQL 查询       |
| POST   | `/api/admin/llm/v1/chat/completions`         | Admin | 平台 LLM 代理             |
| GET    | `/api/admin/llm/keys`                        | Admin | LLM Key 池列表            |
| POST   | `/api/admin/llm/keys`                        | Admin | 新增 Key                  |
| PATCH  | `/api/admin/llm/keys/:id`                    | Admin | 更新/启停/轮换 Key        |
| DELETE | `/api/admin/llm/keys/:id`                    | Admin | 硬删 Key                  |

### Internal

| 方法 | 路径                           | 鉴权           | 说明           |
| ---- | ------------------------------ | -------------- | -------------- |
| POST | `/api/internal/users/presence` | Internal Token | Track 回写在线 |

---

## 9. 与实现相关的对接注意

1. **App 每次登录都会使旧 App JWT 失效**（含同设备重登），客户端须始终使用最新 `token`。
2. 业务错误看 **HTTP 状态 + `code`**，不要假设失败也是 200。
3. 管理端升管理员：先 `PATCH role=1`，再 `PUT .../admin-password`（密码 ≥8）。
4. 版本 `DELETE` = 下架，记录仍在列表中（`isPublished=false`）。
5. 大屏「今日」按 `BUSINESS_TZ`（默认上海）日历日统计。
6. 大屏 DAU/页面/性能/崩溃走 `/api/admin/track/*`，须配置 `TRACK_BASE_URL` 且 Token 与 Track 一致。

---

## 10. 变更记录

| 日期       | 说明                                                                                                                                      |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-11 | 初稿概览（待评审）                                                                                                                        |
| 2026-08-12 | **按当前实现全量同步为可用契约**：补 `/health`、限流、固定 HTTP 语义；对齐登录 bump Token、gender 校验、删帖码、版本下架、大屏 key/时区等 |
| 2026-08-14 | 增加 `POST /api/internal/users/presence`；`INTERNAL_TOKEN`；去掉「心跳本期不做」                                                          |
| 2026-08-15 | 增加 `/api/admin/track/*` 代理 Track Admin 指标；错误码 `50201`/`50301`；`TRACK_BASE_URL`                                                 |
| 2026-08-16 | 增加 `POST /api/admin/llm/v1/chat/completions`、`POST /api/admin/track/query`（Track 只读 SQL）                                           |
