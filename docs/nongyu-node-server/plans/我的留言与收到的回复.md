# 实施计划：我的留言与收到的回复（Node 侧摘录）

| 项   | 内容                                                                                                                          |
| ---- | ----------------------------------------------------------------------------------------------------------------------------- |
| 说明 | 完整统一计划见 `docs/nongyu-rn-app/plans/我的留言与收到的回复.md`（含 Node 阶段 1）。本文件仅作 Node 目录索引，避免双份漂移。 |
| Spec | `docs/nongyu-node-server/specs/我的留言与收到的回复.md`                                                                       |
| 状态 | **已实现**（与 RN 计划同步）                                                                                                  |

Node 实施要点：

1. `postReplies.repo.ts` 新增 received / sent 分页查询（过滤见 Spec §4）。
2. `appMyPostRepliesRouter` 注册 `GET /received`、`GET /sent`；`/new` 不动。
3. `tests/postReplies.test.ts` 覆盖 Spec §8；同步 `接口文档概览.md`。
