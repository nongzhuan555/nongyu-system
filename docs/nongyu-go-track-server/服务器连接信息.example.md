# Track 服务器连接信息（模板 · 可提交）

复制为 `服务器连接信息.md` 后填写真实值。  
**`服务器连接信息.md` 已加入 `.gitignore`，禁止提交。**

---

## SSH

| 项       | 填写                                             |
| -------- | ------------------------------------------------ |
| Host     | `<公网 IP>`                                      |
| User     | `root`（或专用部署用户）                         |
| 认证     | 口令 / 私钥（优先私钥）                          |
| 口令     | （仅写在本机副本；改密后更新）                   |
| 私钥路径 | 如 `C:\Users\<you>\.ssh\id_ed25519_nongyu_track` |

## 远程路径（装机后填写）

| 项           | 建议默认                              |
| ------------ | ------------------------------------- |
| 二进制       | `/usr/local/bin/nongyu-track`         |
| 数据目录     | `/var/lib/nongyu-track/`              |
| 备份目录     | `/var/backups/nongyu-track/`          |
| 环境文件     | `/etc/nongyu-track.env`               |
| 监听         | `127.0.0.1:8081`                      |
| systemd unit | `nongyu-track.service`                |
| Nginx        | `/etc/nginx/conf.d/nongyu-track.conf` |
| 对外 URL     | `https://<公网IP>`                    |

## 机器探活记录

| 日期 | `uname -a` / 备注 |
| ---- | ----------------- |
|      |                   |
