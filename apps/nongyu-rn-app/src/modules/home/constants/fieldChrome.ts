/**
 * 首页「字段条」共用视觉：通知栏 ↔ 网站搜索框
 * 描边略深于色板 outline，避免在顶部绿渐变上发飘、和白区对比不一致
 */
export const HOME_FIELD_CHROME = {
  height: 40,
  radius: 12,
  borderWidth: 1,
  borderColor: "rgba(10, 124, 89, 0.16)",
  frost: "rgba(255, 255, 255, 0.72)",
} as const;
