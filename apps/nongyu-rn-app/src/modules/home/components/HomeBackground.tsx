import { TabScreenBackground } from "@/components/navigation/TabScreenBackground";

/**
 * @deprecated 请直接用 TabScreenBackground；保留别名避免旧 import 断裂
 */
export function HomeBackground() {
  return <TabScreenBackground />;
}
