# 官网 RUM / PV 上报
# 开发：VITE_TRACK_WEB_URL 可填 /v1/track/web/events（默认亦为此），由 Vite 代理到 VITE_TRACK_PROXY_TARGET
# 生产：推荐同默认同源 /v1/track/web/events + 官网 Nginx 反代 Track；也可填 Track 公网完整 URL（须 Track CORS）
# 留空时客户端仍默认同源 URL；但 Site Key 必填，否则跳过上报
VITE_TRACK_WEB_URL=/v1/track/web/events
# 开发代理目标（仅 vite dev 使用，生产构建忽略）
VITE_TRACK_PROXY_TARGET=https://47.108.74.61
# 与 Track 环境变量 TRACK_WEB_SITE_KEY（或 WEB_SITE_KEY）一致；会打进前端包
# 也可用部署时的 /rum-config.js（见 public/rum-config.example.js）覆盖
VITE_TRACK_WEB_SITE_KEY=
# 可选站点版本串
VITE_SITE_VERSION=web-site
